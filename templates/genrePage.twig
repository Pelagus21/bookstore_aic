<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Genre Page</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        .row-item {
            margin: 15px;
        }
        li:last-child {
            visibility: hidden;
        }
    </style>
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-light bg-info text-white container-fluid mb-5">
    <div class="container-fluid flex-row">
        <a class="navbar-brand" href="\home">Home</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <form class="d-flex">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-light ml-4" type="submit">Search</button>
        </form>
    </div>
</nav>

<div style="margin-top: 50px;" class="container">
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6" style="border: 1px solid #9C9C9C; background-color: #EAEAEA;">
            <div class="row" style="margin-top: 10px">
                <div class="col-md-12">
                    <h2 class="text-center text-info" style="padding-top: 10px"style="padding-top: 10px">{{ genre.Genre_name }}</h2>
                </div>
            </div>
            <div class="row row-item">
                <div class="col-md-12">
                    <h4 style="display: inline;">Authors: </h4>
                    <ul>
                        {% for author in authors %}
                            <li class="h6" style="display: inline;">
                                <a href="/author/{{ author.Id }}">{{ author.First_name}} {{ author.Surname }}</a>
                            </li>
                            <li class="h6" style="display: inline;"><a>,</a></li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
            <div class="row row-item">
                <div class="col-md-12">
                    <h4 style="display: inline;">Books: </h4>
                    <ul>
                        {% for book in books %}
                            <li class="h6" style="display: inline;">
                                <a href="/book/{{ book.Id }}">{{ book.Book_name }}</a>
                            </li>
                            <li class="h6" style="display: inline;"><a>,</a></li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
            <div class="row row-item">
                <div class="col-md-12">
                    <h4 style="display: inline;">Description: </h4>
                    <p style="margin-top: 6px">{{ genre.Description }}</p>
                </div>
            </div>
            <div class="row row-item">
                <div class="col-md-12">
                    <!--<button type="submit" class="btn btn-primary" style="margin-top: 15px; margin-bottom: 15px; float: right;" formaction="/home">Back</button>-->
                    <a href="/home" class="btn btn-primary" style="margin-top: 15px; margin-bottom: 15px; float: right;">Back</a>
                </div>
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>
</div>
</div>
</body>

</html>